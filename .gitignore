# Never publish personal journal data — the skill is shareable, the reflections are not.
# Note the leading slashes: these must NOT match skills/journal/, which is part of the repo.
/journal/
**/journal/reflections/
**/journal/distilled/
**/distilled/preferences.md

# Local agent state
.claude/settings.local.json
.claude/plans/

# Personal cross-project workflow, not repo content.
# NEXT.md is hand-written input to a PORTFOLIO.md generated one level up.
NEXT.md

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
